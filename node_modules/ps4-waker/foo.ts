#!/usr/bin/env ts-node

/* eslint-disable */

import StoreClient from './lib/store';

(async () => {
    const c = new StoreClient();
    const results = await c.search('red dead redemption');

    for (const r of results) {
        console.log(r);
    }
    console.log("found ", results.length);
})();
